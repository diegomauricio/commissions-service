annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output
application.title=servicecomisiones
application.vendor=floresdi
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# Files in build.classes.dir which should be excluded from distribution jar
dist.archive.excludes=
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/servicecomisiones.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.connectionmanager.jar=lib/connectionmanager.jar
includes=**
jar.compress=false
javac.classpath=\
    ${reference.excelbuilder.jar}:\
    ${reference.md5generator.jar}:\
    ${reference.excelupdater.jar}:\
    ${reference.taskmanager.jar}:\
    ${reference.querycopier.jar}:\
    ${reference.csvgenerator.jar}:\
    ${file.reference.connectionmanager.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.external.vm=true
javac.processorpath=\
    ${javac.classpath}
javac.source=1.7
javac.target=1.7
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javac.test.processorpath=\
    ${javac.test.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=servicecomisiones.TigoMedia
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
project.csvgenerator=../csvgenerator
project.excelbuilder=../excelbuilder
project.excelupdater=../actualizadorExcel
project.md5generator=../md5generator
project.querycopier=../querycopier
project.taskmanager=../taskmanager
reference.csvgenerator.jar=${project.csvgenerator}/dist/csvgenerator.jar
reference.excelbuilder.jar=${project.excelbuilder}/dist/excelbuilder.jar
reference.excelupdater.jar=${project.excelupdater}/dist/excelupdater.jar
reference.md5generator.jar=${project.md5generator}/dist/md5generator.jar
reference.querycopier.jar=${project.querycopier}/dist/querycopier.jar
reference.taskmanager.jar=${project.taskmanager}/dist/taskmanager.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project.
# You may also define separate properties like run-sys-prop.name=value instead of -Dname=value.
# To set system properties for unit tests define test-sys-prop.name=value:
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
